[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: marks tests that require a live PostgreSQL database
    external: marks tests that require external service integrations
